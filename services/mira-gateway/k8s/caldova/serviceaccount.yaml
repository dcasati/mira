apiVersion: v1
kind: ServiceAccount
metadata:
  name: mira-gateway
  namespace: mira
  annotations:
    # id-mira-gateway-caldova's client ID (rg-mira-caldova, Caldova
    # subscription/tenant). This identity is entirely separate from the
    # original id-mira-gateway in the dcasati.net subscription -- a fresh
    # federated identity was created for this environment since it's a
    # different Entra tenant.
    azure.workload.identity/client-id: "d67093ec-d1b8-4761-9264-ae922ef53d86"
